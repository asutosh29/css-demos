services:
  lecture-frontend:
    image: node:20-alpine
    container_name: lecture-frontend
    working_dir: /app
    ports:
      - "5173:5173"
    volumes:
      - .:/app
    command: npm start
    restart: unless-stopped
