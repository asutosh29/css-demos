version: '3.8'

services:
  lecture-frontend:
    image: nginx:alpine
    container_name: lecture-frontend
    ports:
      - "5173:6767"
    volumes:
      - ./dist:/usr/share/nginx/html:ro
    restart: unless-stopped
